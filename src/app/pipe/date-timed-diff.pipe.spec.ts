import { DateTimedDiffPipe } from './date-timed-diff.pipe';

describe('DateTimedDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimedDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
